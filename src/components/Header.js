import React from 'react';

const Header = props => (
    <header className="header">
        <h3 className="title">Crop <span>Scouting </span>Notes</h3>

    </header>
);

export default Header;